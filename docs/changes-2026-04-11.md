# 2026-04-11 修改记录

## 修改文件列表

### 1. apps/admin/components/FileUpload.tsx
**变更类型:** 修复 Bug

**问题:** 文件上传区域点击无响应

**修改内容:**
- 引入 `useRef` hook
- 添加 `fileInputRef` 引用 input 元素
- 外层 div 添加 `onClick={() => fileInputRef.current?.click()}`
- 移除 `id` 和 `htmlFor` 属性（不再需要）
- input 添加 `ref={fileInputRef}`

**代码变更片段:**
```tsx
// 新增 import
import { useState, useCallback, useRef } from 'react';

// 新增 ref
const fileInputRef = useRef<HTMLInputElement>(null);

// 外层 div 添加点击事件
<div
  onClick={() => fileInputRef.current?.click()}
  onDragOver={handleDragOver}
  ...
>
  <input
    ref={fileInputRef}
    type="file"
    ...
  />
```


### 2. apps/admin/app/inquiries/page.tsx
**变更类型:** 功能增强

**新增功能:**
- 回复方式显示列
- SLA 超时警告（24小时未回复）
- 相对时间显示
- 超时行背景高亮

**修改内容:**

#### 2.1 新增图标 import
```tsx
import {
  ...,
  Phone, Linkedin, AlertTriangle
} from 'lucide-react';
```

#### 2.2 新增回复方式配置
```tsx
const REPLY_METHOD_CONFIG = {
  EMAIL: { icon: Mail, ..., label: '邮件' },
  WHATSAPP: { icon: MessageSquare, ..., label: 'WhatsApp' },
  PHONE: { icon: Phone, ..., label: '电话' },
  ALIBABA: { icon: ShoppingBag, ..., label: 'Alibaba' },
  LINKEDIN: { icon: Linkedin, ..., label: 'LinkedIn' },
  OTHER: { icon: Globe, ..., label: '其他' },
};
```

#### 2.3 更新 Inquiry 接口
```tsx
interface Inquiry {
  ...,
  repliedAt?: string | null;
  repliedBy?: string | null;
  replyMethod?: keyof typeof REPLY_METHOD_CONFIG | null;
  activities?: Activity[];
}
```

#### 2.4 新增辅助函数
- `getRelativeTime()` - 计算相对时间（X分钟前/X小时前/X天前）
- `isOverdue()` - 检查是否超过24小时未回复
- `getOverdueHours()` - 计算超时小时数

#### 2.5 更新表格列
**新增列:**
- 回复方式
- 耗时（含超时警告）

**修改 Thead:**
```tsx
<th>回复方式</th>
<th>耗时</th>
```

#### 2.6 超时视觉效果
- 整行背景: `bg-red-50/30`
- 警告图标: `AlertTriangle` + `animate-pulse`
- 超时文字: 红色加粗


### 3. docs/work-progress-2026-04-11.md
**变更类型:** 新增

**内容:** 今日工作进度记录


### 4. docs/changes-2026-04-11.md
**变更类型:** 新增

**内容:** 本文件 - 详细代码变更记录

---

## 测试验证

### FileUpload 组件
✅ 图片上传区域点击正常
✅ 视频上传区域点击正常
✅ 文件选择器正确弹出

### 询盘列表页
✅ 新增列正确渲染
✅ 超时警告正确显示
✅ 回复方式图标正确显示
